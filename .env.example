# Optional: only needed if you want guest wishes shared between every visitor.
# Without these, the invitation still works and wishes are stored in each visitor's browser.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
